{
  "schema_version": 1,
  "last_run_at": "2026-08-16T17:09:13Z",
  "themes": [
    {
      "id": "barrel-tree-shaking",
      "title": "Barrel and tree-shaking shape",
      "question": "What does a consumer who imports one Button actually download, and does the barrel in src/index.ts plus package.json sideEffects let the rest be shaken out?",
      "surfaces": ["src/index.ts", "package.json", "vite.config.ts"],
      "status": "done",
      "source": "seed",
      "added_at": "2026-08-07T00:00:00Z",
      "last_investigated_at": "2026-08-07T19:09:05Z",
      "findings": [52],
      "notes": "Ranked #1: surfaces join to the whole export table (barrel totalBytes 145,597 B gz, the largest measured surface). Verdict: per-export tree-shaking WORKS. sideEffects:['**/*.css'] is effectively false for the JS graph (no .ts/.tsx imports .css); preserveModules emits one dist file per module; @base-ui via subpaths; lucide per-icon; type-only imports erased; ogl reachable ONLY via the Aurora export (aurora.tsx imported by no other non-story module); seasons decoupled from chrome (import type). The one cross-cutting gap = no per-component subpath exports despite dist already emitting the files -> RSC/imperfect bundlers traverse the whole barrel. Filed #52. Deferred to sibling themes: (a) ogl as a hard `dependency` vs optional peer = install-weight, belongs to ogl-reachability; (b) the ~8.5 KB tailwind-merge/clsx `cn` floor is counted per-export in the snapshot but deduped in real apps, so it is a reading caveat, not a defect (not filed, would be a false positive)."
    },
    {
      "id": "ogl-reachability",
      "title": "ogl reachability from the main entry",
      "question": "Is the WebGL dependency reachable from the package entry point, and can it move behind a lazy or split path so consumers who never render Aurora do not pay for it?",
      "surfaces": ["src/components/effects/aurora.tsx", "src/components/seasons/**", "src/index.ts"],
      "status": "pending",
      "source": "seed",
      "added_at": "2026-08-07T00:00:00Z",
      "last_investigated_at": null,
      "findings": [],
      "notes": "The bundle snapshot records reachable externals per export — check it before reasoning about the module graph by hand."
    },
    {
      "id": "animation-loop-policy",
      "title": "Animation loop policy across every driven component",
      "question": "Do all rAF, WebGL, and timer loops honour prefers-reduced-motion, pause on visibilitychange, and stop when off-screen? Treat this as one policy, not as a per-file question.",
      "surfaces": ["src/components/effects/aurora.tsx", "src/components/seasons/**", "src/components/effects/react-bits/**"],
      "status": "pending",
      "source": "seed",
      "added_at": "2026-08-07T00:00:00Z",
      "last_investigated_at": null,
      "findings": [],
      "notes": "vrt/skip.json documents that Aurora's loop is free-running and does not honour prefers-reduced-motion. Fixing that also makes the component baseline-able, which closes a VRT coverage hole."
    },
    {
      "id": "resource-disposal",
      "title": "Resource disposal sweep",
      "question": "Does every GPU resource, timer, observer, and listener have a teardown path on unmount?",
      "surfaces": ["src/components/effects/aurora.tsx", "src/components/seasons/**", "src/components/effects/react-bits/**"],
      "status": "pending",
      "source": "seed",
      "added_at": "2026-08-07T00:00:00Z",
      "last_investigated_at": null,
      "findings": [],
      "notes": "The bench CANNOT help with this theme yet: retention is measured but uncalibrated, so every verdict is 'unknown' and the raw slopes track story size rather than leaks. Disposal must be argued from the teardown code itself. Do not quote a retention number as evidence of a leak."
    },
    {
      "id": "rerender-cascades",
      "title": "Re-render cascade mapping",
      "question": "Where does a parent re-render cascade across component boundaries, and which context values or prop identities cause it?",
      "surfaces": ["src/components/board/**", "src/components/chrome/**"],
      "status": "done",
      "source": "seed",
      "added_at": "2026-08-07T00:00:00Z",
      "last_investigated_at": "2026-08-16T17:09:13Z",
      "findings": [63],
      "notes": "Ranked #1: surfaces join to the most expensive component exports (BoardSelector 73,530 B gz — largest single component; LanguageSelector 60,142; Sidebar 57,492) and the highest-mount bench stories (chrome-appshell--collapsed 55.5ms, chrome-sidebar--ai-active 52.7ms, board-scaledboarddisplay 47.1ms). VERDICT: RESOLVED at the library level — nothing new to file. This theme was already investigated in a prior run and filed as #63 (CLOSED/COMPLETED, not rejected; rejected-findings.jsonl is empty), whose fixes are all present in the current tree: (a) every chrome component is now memo'd — sidebar, board-selector, language-selector, theme-toggle, main-content, page-header/page-layout/page-toolbar, fiesta-logo, board-icon; (b) the Select context churn is stabilized via lib/select-items stabilizeSelectItems + render-phase state adjustment (#62), so SelectItemsContext value and Base UI `items` stay referentially stable; (c) Sidebar's window-resize appInset listener is now rAF-coalesced and bails when the inset is unchanged (sidebar.tsx:275-296), and the ResizeObserver header-height publish is likewise coalesced + skips unchanged writes. The board render path is triple-protected against cascades: BoardDisplay + CharTile + GridRow + StaticGridRow + StaticTile all memo'd with custom comparators; the grid array is useMemo'd and rows are reference-stable so a BoardDisplay re-render does not reach the tiles; flapSpeed is compared via resolveFlapSpeed so a fresh {durationMs} object does not re-render; loading uses one shared ticker per cadence instead of ~132 per-tile intervals; board-surfaces.ts materials and board-metrics class maps are module-scope. The seasonal auroras (SidebarAurora/SidebarAuroraHorizontal, rendered directly by Sidebar) are memo'd on a stable `colors` ref. The ONLY residual cascade is consumer-side prop identity: the memo #63 added to the chrome shell only bails if the consuming app passes referentially-stable slot/array/render-prop values (Sidebar.boardSelector/mobileBoardSelector/versionSlot/themeToggleSlot/primaryItems/secondaryItems/renderLink/ai, MainContent.children). At the AppShell demo composition these are recreated on every shell state change, so memo does not bail — but that is not a library defect (a presentational/controlled library cannot stabilize consumer-supplied identities) and #63 explicitly deferred it as the separate discovered theme `prop-contract-stability`. Not refiled: doing so would re-propose a finding #63 already deferred AND belongs to the 'memo'd component + unstable consumer props' false-positive class. Considered and rejected as a finding: ScaledBoardDisplay uses React's default shallow memo while wrapping the custom-comparator BoardDisplay — this is the normal memo pattern (stable for the default flapSpeed='standard'/no-custom-label case) and BoardDisplay's comparator backstops the tiles regardless, so it is not a defect."
    },
    {
      "id": "css-paint-budget",
      "title": "CSS paint and layer budget",
      "question": "Which styles promote layers, force paint, or use unbounded will-change, and what does that cost across the whole stylesheet rather than per component?",
      "surfaces": ["src/styles/**"],
      "status": "pending",
      "source": "seed",
      "added_at": "2026-08-07T00:00:00Z",
      "last_investigated_at": null,
      "findings": [],
      "notes": "Large blur radii and layer promotion show up in the bench as frame p95 above the vsync floor."
    },
    {
      "id": "prop-contract-stability",
      "title": "Prop-contract stability for the presentational shell",
      "question": "Now that the chrome shell is uniformly memo'd (#63), its memos only bail when the consuming app passes referentially-stable slot/array/render-prop values — which the documented AppShell composition does not. Should the library close this at its own boundary (a composed AppShell that owns stable slot/callback identities, and/or a stabilization hook) rather than leaving every consumer to hand-memoize, and what does the un-bailed cascade actually cost per shell state change?",
      "surfaces": ["src/components/chrome/**", "src/components/forms/select.tsx"],
      "status": "pending",
      "source": "discovered",
      "added_at": "2026-08-16T17:09:13Z",
      "last_investigated_at": null,
      "findings": [],
      "notes": "Referenced by #63 as a tracked-separate discovered theme but never persisted to this file (state write-back gap). The residual is genuinely consumer-side for a presentational library, so a naive 'memoize your props' issue against the library is a false positive — the run must instead evaluate whether the library should OWN the stable identities (a composed <AppShell> that memoizes slots/callbacks internally, or a documented stabilization helper), and measure the real per-interaction cost first. A human should prune this if the answer is 'leave it to consumers'."
    }
  ]
}
