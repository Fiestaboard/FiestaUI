# eslint-plugin-jsx-a11y has not yet added eslint 10 to its peer range —
# same situation as FiestaBoard's web/ (which passes --legacy-peer-deps in
# CI and Docker). Repo-level so plain `npm ci` works everywhere.
legacy-peer-deps=true
